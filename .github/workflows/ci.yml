name: CI/CD Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-test-deploy:
    runs-on: self-hosted
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    # [수정] setup-python 제거 (Mac Mini에 설치된 시스템 Python 사용)
    # - name: Set up Python 3.10
    #   uses: actions/setup-python@v4 ...

    - name: Install dependencies
      run: |
        # Mac에서는 python3 명령어를 명시적으로 사용
        python3 -m pip install --upgrade pip
        if [ -f requirements.txt ]; then python3 -m pip install -r requirements.txt; fi

    - name: Lint with flake8
      run: |
        # python3 -m flake8 형태로 실행하여 경로 문제 방지
        python3 -m flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        python3 -m flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Run Unit Tests
      run: |
        python3 -m pytest tests/

    - name: Deploy to Mac Mini Server
      if: github.event_name == 'push'
      run: |
        echo "🚀 Starting Deployment on Mac Mini..."
        
        # [최적화] 변경된 'api-server'만 빌드 (MLflow 등은 재빌드 안 함)
        docker-compose -f docker/docker-compose.yml build api-server
        
        # 변경된 서비스만 교체하고 나머지는 유지
        docker-compose -f docker/docker-compose.yml up -d --no-deps api-server
        # 혹시 꺼져있는 다른 서비스가 있다면 켜주기 (선택 사항)
        docker-compose -f docker/docker-compose.yml up -d
        
        echo "✅ Deployment Successful! Service is updated."

    - name: 🔍 Debugging - Check Docker Status
      if: always()
      run: |
        echo "=== 1. Docker Container Status ==="
        docker ps -a
        
        echo "\n=== 2. MLflow Logs (Last 50 lines) ==="
        docker-compose -f docker/docker-compose.yml logs --tail=50 mlflow_server || echo "Failed to get logs"
        
        echo "\n=== 3. Port Usage (5000/5001) ==="
        lsof -i :5000 || echo "Port 5000 is free"
        lsof -i :5001 || echo "Port 5001 is free"
